import { CreateCampaignPage } from "@/components/ads/create-campaign-page";

export const metadata = {
  title: "Create Campaign | Ads",
};

export default function Page() {
  return <CreateCampaignPage />;
}
